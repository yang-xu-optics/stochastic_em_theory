# Results Workspace

Generated simulation outputs, figures, and run manifests belong here.

Recommended layout:

- `runs/`: per-run outputs and manifests.
- `figures/`: paper-ready or wiki-ready figures.

Large temporary outputs should go under `tmp/` or another ignored directory if
they do not need to be committed.

